{
  "name": "@social-autopilot/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run --outdir=dist",
    "cf:dev": "wrangler dev",
    "cf:deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@social-autopilot/ai": "workspace:*",
    "@social-autopilot/core": "workspace:*",
    "@social-autopilot/database": "workspace:*",
    "@social-autopilot/social": "workspace:*",
    "hono": "^4.7.4",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250317.0",
    "@social-autopilot/config": "workspace:*",
    "typescript": "^5.8.2",
    "vitest": "^3.0.9",
    "wrangler": "^4.1.0"
  }
}
